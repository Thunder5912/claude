# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here

# Logging Configuration
LOG_LEVEL=INFO

# Optional: Custom download directory (default: ./downloads)
# DOWNLOAD_DIR=/app/downloads

# Optional: Maximum file size in bytes (default: 4GB)
# MAX_FILE_SIZE=4294967296

# Optional: Maximum concurrent downloads (default: 3)
# MAX_CONCURRENT_DOWNLOADS=3

# Optional: Torrent timeout in seconds (default: 300)
# TORRENT_TIMEOUT=300

# Optional: Progress update interval in seconds (default: 5)
# PROGRESS_UPDATE_INTERVAL=5

# Optional: Upload timeout in seconds (default: 600)
# UPLOAD_TIMEOUT=600

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your_telegram_bot_token_here' with your actual bot token
# 3. Adjust other settings as needed
# 4. Never commit the .env file to version control
